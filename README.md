# MobileNet-Replication
MobileNet-Replication is a PyTorch project that reconstructs the MobileNet architecture in a theoretical, implementable form. The model uses depthwise separable convolutions and hyperparameters (width and resolution multipliers) to provide a low-computation, mobile-optimized network.
